apiVersion: v1
kind: Service
metadata:
  name: mecanica-api
  namespace: mecanica
spec:
  selector:
    app: mecanica-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: LoadBalancer
